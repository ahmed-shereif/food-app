namespace Presentation.ViewModels.RecipeViewModel
{
    public record GetAllRecipesViewModel(int id,string Name, string ImageUrl, decimal Price, string Description, string Tag, string Category);
    
}
